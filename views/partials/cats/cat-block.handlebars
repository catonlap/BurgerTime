<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}DEVOUR{{else}}ADD TO PLATE{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">REMOVE</button>
</li>
